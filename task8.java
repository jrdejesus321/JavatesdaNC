import java.util.ArrayList;
import java.util.List;

public class task8 {
    public static void main(String[] args) {
        System.out.println(arrith(10, 2,4));
    }

    static List arrith(int a, int b, int c){
        List arra = new ArrayList<>();
        List arrb = new ArrayList<>();
        List arrc = new ArrayList<>();

        for (int x = 1; x <= a; x++) {
            arra.add(x);
        }
        for (int x = 1; x <= b; x++) {
            arrb.add(x);
        }
        for (int x = 1; x <= c; x++) {
            arrc.add(x);
        }

        List all = new ArrayList<>();

    return all;
    }
}
